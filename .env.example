# Optional: require x-api-token on write endpoints when set.
# API_TOKEN=change-me

# Optional overrides; defaults are suitable for the bundled container.
# PORT=8080
# DATA_DIR=/app/data
# DB_PATH=/app/data/tracker.db
